// lib/domain/entities/article.dart

class Article {

  String ? id;
  String ? parcid;
  String ? designation;
  String ? us;
  String ? quantity;
  String ? type;

  Article({
    this.id,
    this.parcid,
    this.designation,
    this.us,
    this.quantity,
    this.type,
  });
  
}
